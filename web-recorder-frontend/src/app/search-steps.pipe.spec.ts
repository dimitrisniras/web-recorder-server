import { SearchStepsPipe } from './search-steps.pipe';

describe('SearchStepsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStepsPipe();
    expect(pipe).toBeTruthy();
  });
});
